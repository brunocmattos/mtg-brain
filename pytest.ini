[pytest]
# repo root no sys.path pra `import mtg_brain` funcionar tanto com `pytest`
# quanto com `python -m pytest` (o CI usa `pytest` puro, que NAO poe o CWD no path).
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
